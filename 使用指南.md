# 耕地标注工作台 · 使用指南

面向第一次上手的朋友。工作台是一个**常驻侧边栏**（不挡地图），
核心思路是"减法勾绘"：每个渔网格先铺一整块底板，再把道路、城镇、
坑塘等非耕地**挖掉**、漏画的**补上**，剩下的就是耕地。

## 0. 打开工作台

菜单 **矢量 → Cropland Delineator → 勾选「耕地标注工作台」**。
侧栏出现在右侧，可以拖到左边或浮动，位置会被记住。

---

## 1. 加载标注数据（每个工程一次）

展开侧栏的 **「① 标注数据」**（默认折叠）：

1. **渔网图层**：下拉选你的样方渔网（面图层）；
2. **影像**：下拉选卫星影像（磁力走线和点选分割要用）；
3. **标注文件**：路径一般会自动填好（渔网同目录的 `耕地标注.gpkg`），
   没有就点「浏览」选一个；点 **「打开 / 新建标注层」**。

之后**不用每次重复**：下次打开 QGIS 再开工作台，会自动接续上次的
渔网、标注文件和工作格，不会再叠出重复图层。

## 2. 加载格子（逐格工作流的起点）

1. 用 QGIS 的**矩形选择工具**，在渔网图层里选中一个格子；
2. 点 **「选中格子 → 建底板」**：整格铺上底板（橙色），台账开始记录；
3. **「缩放到格子」**把视野对准当前格；
4. 一格做完后按 **N**（或点「➡ 下一格」）自动跳到下一格。

已标过的格子：选中后再点「建底板」即切回该格继续。

## 3. 路网拉取（OSM 道路先验）

适合先用公共路网把大路扣掉，再手动处理细部：

1. 点 **「🌐 拉取 OSM 道路」**（走 Overpass，几十秒，结果会缓存）；
2. 侧栏上方的**「宽度档：大路 / 小路」**设定两档道路全宽（米）；
3. 点 **「🛣 按 OSM 道路挖除当前格」**：路网按类别分档缓冲后一次性
   从底板扣除。扣多了不用怕——台账里能单步删除（见 §8）。

## 4. 挖除（扣掉非耕地）

四种方式，按形状选：

| 场景 | 用法 |
|---|---|
| **🛣 道路**（含磁力道路 R，见 §5） | 沿路点中心线，按当前档宽缓冲扣除；画线时按住 Shift 正交锁 |
| **⬛ 框挖** | 按住左键拖一个框，松开即扣除（城镇等连片建设区） |
| **🟥 多边形挖** | 逐点画多边形（林地、水域等不规则区域），右键/Enter 收笔 |
| **⭕ 选中即挖除 (E)** | 别的图层里画好的/生成的多边形，选中后按 E 直接当橡皮用 |

共同的键盘习惯：**右键或 Enter 收笔，Esc 取消，Backspace / Ctrl+Z 退点**；
道路类按 `1`/`2` 切大/小路档，`[` `]` 微调宽度。

## 5. 磁力切分（M）、磁力道路（R）与磁力补画

大片耕地内部要按田埂/小路分块时用**磁力切分**：

1. 按 **M** 激活，在地块一侧点**起点**，另一侧点**终点**；
2. 松开鼠标让预览走一下——虚线是建议线（沿影像上真实的田埂/道路走），
   鼠标再点一下即可采纳；不满意就再移动鼠标重算；
3. 继续点点子延伸，右键/Enter 收笔，一条线把大地块从缝隙处分开；
4. **磁力道路（R）** 同理，但走的是道路中心线并按档宽扣除。

技巧：计算有范围框（青色），太长的线分两笔画；退点用 Backspace。

**磁力补画（🧲🟩）**：整块耕地从零开始标注时用——沿目标边界点一圈顶点，
每一段都自动吸到影像上的田埂/边界（虚线=建议线），右键/Enter 闭合落地
成整块耕地。按 `1` 落地为补画（绿），`2` 落地为挖除（红），
Backspace / Ctrl+Z 退点，Esc 取消。收笔时会自动补算首尾之间的闭合段。
没有影像时退化为普通多边形，照样能用。

## 6. 补画（🟩）

建筑密集等底板铺不满的格子用加法：点 **「🟩 补画」**，逐点画多边形，
收笔后自动裁回格子范围，作为一块耕地加入。

## 7. 点选分割（🖱 点选）——最快的一种

配好 nn/ 环境后可用（见仓库 README；没配也不影响其他功能）：

1. 点 **「🖱 点选」**，在**目标内部点一下左键**（正点），AI 沿影像边界
   实时给出掩码预览（首次每格需几秒载入影像，之后每击约 0.1~0.3 秒）；
2. 粘到别的东西？**右键点一个负点**排除；多点几个正点可以修形状；
3. 按 **`1` = 补画（绿）/ `2` = 挖除（红）** 决定落地方式，**Enter 落地**；
4. 退点 Backspace / Ctrl+Z，取消 Esc。

适合规则田块一击即中；复杂形状配合负点修正。

## 8. 台账——改错的两种办法

每一笔（底板/道路/框挖/挖除/补画/切分/OSM）都记入台账，可回放。

- 点 **「⏱ 台账…」**：列表选中某步——
  - **↩ 回到此步**：该步之后的操作全部作废（大撤销）；
  - **🛣 改宽并重放**：改某条道路的宽度，其余操作保留；
  - **✕ 删除此步**：只删这一步，其余保留并自动重放（比如只删某条路）；
- **↩ 撤销一笔** / Ctrl+Z：常规撤销上一笔。

## 9. 分层视图——看见"底板 / 补块 / 扣除"

想直观看当前格是由哪些操作叠出来的、或想图上点删：

1. 点 **「🥞 分层视图」**：出现三个图层——**底板（橙）/ 补块（绿）/
   扣除（红，道路按宽度显示成带）**。当前格耕地 = 底板 + 补块 − 扣除；
2. **图层面板里单击**某个视图层让它高亮（活动图层）；
3. 用矩形选择工具框选要删的区域（变黄即选中，可跨层多选）；
4. 点 **「🗑 删除选中操作」**：对应操作步删除，其余自动重放。

底板步不能删（删它=重开格）。用完再点一下 🥞 关闭视图。

## 10. 每格收尾

- **💾 保存**（关侧栏/切格也会自动保存提交）；
- ⑤ 收尾QA：**🧹 清理碎块**（删小于阈值的碎斑）、**重叠检查**、
  **🕳 漏画检查**（大空洞做成候选层，注意里面含有意挖除的道路/城镇）、
  **💧 暗斑提示**（疑似水体/阴影）、**导出报表 CSV**、**📊 进度总览**。

## 快捷键速查

| 键 | 作用 |
|---|---|
| E | 选中即挖除 |
| N | 下一格 |
| M / R | 磁力切分 / 磁力道路 |
| 1 / 2 | 大路 / 小路宽度档（点选、磁力补画里=补画/挖除） |
| [ / ] | 道路宽度 −1 / +1 米 |
| Shift（画线时） | 正交锁 |
| 右键 / Enter | 收笔 / 落地 |
| Esc | 取消当前笔 |
| Backspace / Ctrl+Z | 退点；点退光后 Ctrl+Z = 撤销上一笔已落地的操作 |

有日志看不懂或报错，把侧栏底部日志区的文字截图发我。

## 附：Windows 常见问题排查（朋友版）

### ① 插件源里能看到插件，但安装/升级失败

- 报错若含 **"Could not store plugin to the plugin directory"** 且路径尾巴是
  `cropland_delineator-0` 之类：旧版发布包文件名用了连字符（`-0.9.3.zip`），
  QGIS 安装器按文件名**第一个点号截断**推导插件目录名（得到 `…-0`），
  与包内真实目录对不上 → 存盘失败。**v0.9.4 起已改为点分隔命名
  （`cropland_delineator.0.9.4.zip`），从插件源安装/升级即可**；装前可把
  插件目录里残留的 `cropland_delineator-0` 空文件夹删掉。
- 若报错是**网络超时/下载不下来**：插件列表能出现说明源连得上，但
  附件下载走 `objects.githubusercontent.com`，国内网络经常不通——
  用浏览器（必要时挂代理）到 release 页下载 zip，再手动装入：
  解压到 `C:\Users\<你>\AppData\Roaming\QGIS\QGIS4\profiles\default\python\plugins\`
  （先删旧的 cropland_delineator 文件夹），重启 QGIS。

### ② 保存时提示"保存失败 / 文件被占用"

先把插件升到 **v0.9.3**：保存失败时日志会打出**真实报错原文**并自动
重试一次（杀毒瞬时锁大多直接重试成功）；关闭工作台时也不再丢弃未
保存的编辑。然后按顺序查：

1. **另一个 QGIS 窗口/工程开着同一个 .gpkg** → 全部关掉，只留一个；
2. **同一工程里这个 gpkg 还有别的图层**（自己加的、浏览器面板拖进来的）
   → 工作台打开时会警告"同一个 GPKG 还有别的图层开着"，把用不到的移除；
3. **杀毒实时扫描**（360/火绒/Defender）→ 把标注成果所在文件夹加白名单；
4. **文件在 OneDrive/网盘同步目录**（文档、桌面常默认同步）→ 把 .gpkg
   挪到纯本地目录（如 D:\标注\）；
5. **QGIS 上次异常退出留下的 `-wal`/`-shm` 小文件** → 完全退出 QGIS 后
   删掉成果 gpkg 旁边的 `xxx-wal`、`xxx-shm` 两个文件再打开。

再失败就把日志里"[错误] 保存失败：…"那一行原文发来——里面有真实原因。
